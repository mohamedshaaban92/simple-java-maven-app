pipeline {
    agent any
    stages {
        stage('display hostname') {
            steps {
                sh "hostname"
            }
        }

        stage('display OS version') {
            steps {
               sh "cat /etc/os-release"
            }
        }

        stage('Display Ip address') {
            steps {
               sh " hostname -I"
            }
        }
    }
    
    post{
            success{
                echo "Job succeeded"
            }
            failure{
                echo "Jib failed"
            }
        }
}
